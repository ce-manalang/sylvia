---
phase: 02-core-solo-experience
plan: 03
type: execute
wave: 3
depends_on:
  - 02-core-solo-experience-02
files_modified:
  - src/components/session/JournalPanel.tsx
  - src/components/session/EmotionTagPicker.tsx
  - src/components/session/SessionScreen.tsx
  - src/components/reflection/ReflectionScreen.tsx
  - src/components/reflection/MoodCheckIn.tsx
  - src/components/reflection/FavoriteAnswer.tsx
  - src/components/reflection/LearnedInsight.tsx
  - src/components/history/HistoryScreen.tsx
  - src/components/history/SessionCard.tsx
  - src/components/settings/SettingsScreen.tsx
  - src/hooks/useJournalStorage.ts
  - src/hooks/useSessionStorage.ts
  - src/App.tsx
autonomous: true
requirements:
  - JRNL-01
  - JRNL-02
  - JRNL-03
  - JRNL-04
  - REFL-01
  - REFL-02
  - REFL-03
  - REFL-04
  - REFL-05
  - UX-04

must_haves:
  truths:
    - "User can write a private journal response to any question during a session"
    - "Journal entries are saved to IndexedDB and accessible in session history"
    - "User can tag entries with emotion tags (happy, sad, thoughtful, connected, etc.)"
    - "End-of-session reflection shows summary with favorite answer, mood check-in, learned insight"
    - "User can view past sessions in history screen with date and deck info"
    - "Settings screen exists with display name edit and preferences"
  artifacts:
    - path: "src/components/session/JournalPanel.tsx"
      provides: "Slide-up journal writing panel during session"
      min_lines: 50
    - path: "src/components/session/EmotionTagPicker.tsx"
      provides: "Emotion tag selection UI with predefined tags"
      min_lines: 30
    - path: "src/components/reflection/ReflectionScreen.tsx"
      provides: "End-of-session summary with all reflection elements"
      min_lines: 80
    - path: "src/components/history/HistoryScreen.tsx"
      provides: "Session history list with past conversations"
      min_lines: 50
    - path: "src/components/settings/SettingsScreen.tsx"
      provides: "Settings screen with display name, preferences"
      min_lines: 40
---

<objective>
Build journaling during sessions, emotion tagging, end-of-session reflection summary, session history view, and settings screen. This completes the solo experience loop.

Purpose: Close the experience loop so users write reflections, see meaningful summaries, and can revisit past sessions. Settings provide basic app configuration.

Output: Full journaling flow, reflection summary, session history, and settings -- completing all Phase 2 requirements.
</objective>

<tasks>

<task type="auto">
  <name>Build journal panel and emotion tag picker for in-session writing</name>
  <files>
    src/components/session/JournalPanel.tsx
    src/components/session/EmotionTagPicker.tsx
    src/hooks/useJournalStorage.ts
    src/components/session/SessionScreen.tsx
  </files>
  <action>
    1. Create src/components/session/JournalPanel.tsx:
       - Slide-up panel that appears when user taps "Write" or journal icon on card
       - Textarea for free-form writing (placeholder: "What comes to mind?")
       - EmotionTagPicker component below textarea
       - Save button that writes to Dexie via useJournalStorage
       - Close/minimize button to return to card view
       - Smooth slide-up animation (transform translateY)
       - Shows current question text at top for context

    2. Create src/components/session/EmotionTagPicker.tsx:
       - Predefined emotion tags: happy, sad, thoughtful, connected, anxious, grateful, vulnerable, hopeful
       - Pill-style buttons, multi-select allowed
       - Selected tags highlighted with level-appropriate color
       - Compact horizontal scroll layout

    3. Update src/hooks/useJournalStorage.ts:
       - Add saveEntry(entry): write journal entry to Dexie
       - Add getEntriesForSession(sessionId): query entries by session
       - Add updateEntryEmotions(entryId, emotions): update emotion tags
       - Keep existing useJournalEntries() for all entries

    4. Update SessionScreen.tsx:
       - Add journal icon/button on each QuestionCard
       - Toggle JournalPanel visibility
       - Pass current question and sessionId to panel
       - After saving entry, show brief confirmation and return to card
  </action>
</task>

<task type="auto">
  <name>Build end-of-session reflection screen</name>
  <files>
    src/components/reflection/ReflectionScreen.tsx
    src/components/reflection/MoodCheckIn.tsx
    src/components/reflection/FavoriteAnswer.tsx
    src/components/reflection/LearnedInsight.tsx
  </files>
  <action>
    1. Create src/components/reflection/MoodCheckIn.tsx:
       - Scale of 5 mood options (emoji or word-based): "Drained", "Neutral", "Calm", "Energized", "Inspired"
       - Single-select, circular buttons
       - Returns selected mood string

    2. Create src/components/reflection/FavoriteAnswer.tsx:
       - List of questions the user wrote entries for during this session
       - User taps to select their favorite
       - Shows question text + preview of their answer
       - Single-select with highlight

    3. Create src/components/reflection/LearnedInsight.tsx:
       - Text input: "One thing I learned about myself..."
       - Optional, can skip
       - Clean, minimal design

    4. Create src/components/reflection/ReflectionScreen.tsx:
       - Props: sessionId, onComplete
       - Load session data and journal entries for this session
       - Vertical scroll layout with sections:
         a. Session complete header with deck name and stats (questions answered, skipped)
         b. MoodCheckIn component
         c. FavoriteAnswer component (if user wrote entries)
         d. LearnedInsight component
         e. "Save & Finish" button
       - On save: write SessionReflection to Dexie, mark session complete
       - Share button: copy summary text to clipboard (REFL-05)
       - Max 3-4 visible elements, calm layout, not overwhelming
  </action>
</task>

<task type="auto">
  <name>Build session history and settings screens</name>
  <files>
    src/components/history/HistoryScreen.tsx
    src/components/history/SessionCard.tsx
    src/components/settings/SettingsScreen.tsx
    src/App.tsx
  </files>
  <action>
    1. Create src/components/history/SessionCard.tsx:
       - Props: session, entryCount, deckName
       - Card showing: date, deck name, questions answered, mood (if reflected)
       - Tap to expand and see journal entries for that session
       - Warm card style consistent with app design

    2. Create src/components/history/HistoryScreen.tsx:
       - Props: onNavigate, onBack
       - Query all completed sessions from Dexie, ordered by date (newest first)
       - Map sessions to SessionCard components
       - Empty state: "No sessions yet. Start a conversation to see your history here."
       - Header with back button

    3. Create src/components/settings/SettingsScreen.tsx:
       - Props: displayName, onUpdateName, onNavigate, onBack
       - Sections:
         a. Profile: edit display name
         b. Preferences: content warnings toggle (show/hide sensitive question warnings)
         c. Data: "Your data is stored locally on this device"
         d. About: app version, "WNRS Companion"
       - Clean list-style layout with section headers

    4. Update src/App.tsx:
       - Wire up all screens: home, session, reflection, history, settings
       - Add reflection screen after session complete
       - Add navigation to history and settings from home
       - Bottom navigation bar with: Home, History, Settings icons
  </action>
</task>

</tasks>
