---
phase: 02-core-solo-experience
plan: 01
type: execute
wave: 1
depends_on:
  - 01-foundation-auth-03
files_modified:
  - src/types/session.ts
  - src/types/journal.ts
  - src/db/schema.ts
  - src/db/init.ts
  - src/data/starter-deck.ts
  - src/hooks/useSessionStorage.ts
  - src/components/home/HomeScreen.tsx
  - src/components/home/DeckCard.tsx
  - src/App.tsx
autonomous: true
requirements:
  - CONV-01
  - DECK-01
  - DECK-04

must_haves:
  truths:
    - "Session and reflection data models exist in Dexie with proper indexes"
    - "Starter deck contains 15+ questions across 3 levels (Perception, Connection, Reflection)"
    - "User can see deck selection on home screen and tap to start a session"
    - "App uses simple state-based routing (home, session, history, settings)"
  artifacts:
    - path: "src/types/session.ts"
      provides: "TypeScript types for Session, Deck, Question, ConversationLevel"
      min_lines: 30
    - path: "src/data/starter-deck.ts"
      provides: "Free starter deck with 15+ questions across 3 levels"
      min_lines: 60
    - path: "src/db/schema.ts"
      provides: "Extended Dexie schema with sessions and reflections tables"
      min_lines: 25
    - path: "src/hooks/useSessionStorage.ts"
      provides: "Hooks for creating/updating/querying sessions"
      min_lines: 30
    - path: "src/components/home/HomeScreen.tsx"
      provides: "Home screen with deck selection and start session button"
      min_lines: 40
    - path: "src/App.tsx"
      provides: "Updated with state-based routing between screens"
      min_lines: 40
---

<objective>
Extend the data layer for conversation sessions and create the starter deck content. Build a home screen with deck selection that allows users to start a solo session.

Purpose: Establish the data foundation and entry point for the core solo experience. Users should be able to see available decks and initiate a session.

Output: Extended database schema, starter deck with 15+ questions across 3 levels, home screen with deck card, and state-based routing.
</objective>

<tasks>

<task type="auto">
  <name>Create session types and extend database schema</name>
  <files>
    src/types/session.ts
    src/types/journal.ts
    src/db/schema.ts
    src/db/init.ts
  </files>
  <action>
    1. Create src/types/session.ts with:
       - ConversationLevel enum: 'perception' | 'connection' | 'reflection'
       - Question interface: { id, text, level, deckId, sensitive? }
       - Deck interface: { id, name, description, questionCount, levels, free }
       - Session interface: { id?, deckId, startedAt, completedAt?, currentQuestionIndex, questionsOrder (string[]), skippedQuestions (string[]), status: 'active' | 'completed' | 'abandoned' }
       - SessionReflection interface: { id?, sessionId, questionId, favoriteAnswer?, learnedInsight?, moodRating?, savedAt }

    2. Update src/types/journal.ts:
       - Add sessionId field to JournalEntry (link entries to sessions)
       - Add level field (ConversationLevel) to know which level the entry was from

    3. Update src/db/schema.ts:
       - Bump to version 2
       - Add sessions table: '++id, deckId, startedAt, status'
       - Add reflections table: '++id, sessionId'
       - Update entries index to include sessionId: '++id, timestamp, questionId, sessionId'

    4. Update src/db/init.ts if needed for migration
  </action>
</task>

<task type="auto">
  <name>Create starter deck data</name>
  <files>
    src/data/starter-deck.ts
  </files>
  <action>
    1. Create src/data/starter-deck.ts with:
       - STARTER_DECK metadata: { id: 'starter', name: 'Getting Started', description, free: true }
       - STARTER_QUESTIONS array with 18 questions (6 per level):
         - Perception (Level 1): Light, surface-level questions about self-awareness
           Examples: "What's something you've been thinking about lately?", "What does a perfect day look like for you?"
         - Connection (Level 2): Deeper questions about relationships and vulnerability
           Examples: "What's something you wish people understood about you?", "When was the last time you felt truly seen?"
         - Reflection (Level 3): Deep introspection questions
           Examples: "What would you tell your younger self?", "What are you most afraid to admit to yourself?"
       - Mark 2-3 questions as sensitive: true
       - Export DECKS array containing the starter deck
       - Export getQuestionsForDeck(deckId) helper
       - Export getQuestionsByLevel(deckId, level) helper
  </action>
</task>

<task type="auto">
  <name>Build home screen and routing</name>
  <files>
    src/components/home/HomeScreen.tsx
    src/components/home/DeckCard.tsx
    src/hooks/useSessionStorage.ts
    src/App.tsx
  </files>
  <action>
    1. Create src/hooks/useSessionStorage.ts:
       - createSession(deckId): creates new session in Dexie, returns session id
       - useActiveSessions(): live query for active sessions
       - usePastSessions(): live query for completed sessions
       - completeSession(sessionId): mark session as completed
       - abandonSession(sessionId): mark session as abandoned

    2. Create src/components/home/DeckCard.tsx:
       - Props: deck, onStart callback
       - Card UI with deck name, description, question count
       - Level indicator showing 3 levels
       - "Start" button with terracotta color
       - Physical card feel: rounded corners, shadow, border

    3. Create src/components/home/HomeScreen.tsx:
       - Props: displayName, onStartSession(deckId), onNavigate(screen)
       - Greeting header: "Hi, {displayName}"
       - Section: "Choose a deck" with DeckCard list
       - Section: Quick links to "Journal" and "Settings"
       - Calm, spacious layout with warm palette

    4. Update src/App.tsx:
       - Add screen state: 'home' | 'session' | 'history' | 'settings'
       - Add activeSessionId state
       - Route to HomeScreen, placeholder for SessionScreen, etc.
       - HomeScreen onStartSession creates session and switches to session screen
  </action>
</task>

</tasks>
