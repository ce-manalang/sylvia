---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 01-foundation-auth-01
  - 01-foundation-auth-02
files_modified:
  - src/App.tsx
  - src/components/layout/AppShell.tsx
  - src/components/welcome/WelcomeScreen.tsx
  - src/hooks/useJournalStorage.ts
  - src/main.tsx
  - vite.config.ts
  - public/manifest.json
  - public/vite.svg
autonomous: false
requirements:
  - INFRA-02
  - INFRA-03
  - UX-05

must_haves:
  truths:
    - "User sees welcome screen on first launch, can enter a display name"
    - "App persists display name and shows personalized greeting on return visits"
    - "User can view app in mobile browsers and see responsive layout (no horizontal scroll)"
    - "App can be installed as PWA and loads offline with cached app shell"
    - "Journal entries persist in IndexedDB and survive page refresh"
  artifacts:
    - path: "src/App.tsx"
      provides: "Root app component with initialization logic and routing/conditional rendering"
      min_lines: 30
    - path: "src/components/layout/AppShell.tsx"
      provides: "Main app layout container (header, content area, footer placeholders)"
      min_lines: 25
    - path: "src/components/welcome/WelcomeScreen.tsx"
      provides: "Onboarding flow: input display name, show welcome message"
      min_lines: 40
    - path: "src/hooks/useJournalStorage.ts"
      provides: "React hook using dexie-react-hooks for live journal entry queries"
      min_lines: 20
    - path: "public/manifest.json"
      provides: "PWA manifest with app metadata, icons, theme colors"
      contains: "start_url"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*App"
    - from: "src/App.tsx"
      to: "src/db/init.ts"
      via: "useEffect calls initializeDatabase on mount"
      pattern: "initializeDatabase\\(\\)"
    - from: "src/components/welcome/WelcomeScreen.tsx"
      to: "src/hooks/useDisplayName.ts"
      via: "Hook call to get/set display name"
      pattern: "useDisplayName\\(\\)"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/hooks/useJournalStorage.ts"
      via: "Component queries journal entries for display"
      pattern: "useJournalEntries\\(\\)"
    - from: "vite.config.ts"
      to: "public/manifest.json"
      via: "PWA plugin references manifest"
      pattern: "manifest.*start_url"
---

<objective>
Create core UI components (welcome screen, app shell) and integrate database/storage hooks. Implement PWA manifest for installability. Build the user-facing foundation for journaling and offline functionality.

Purpose: Deliver a working app shell where users see personalized welcome, the app initializes properly with data persistence, and the PWA is installable and works offline.

Output: Functioning app with welcome flow, responsive layout, and PWA capabilities. All three requirements (INFRA-02, INFRA-03, UX-05) satisfied with working implementation.
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create App component with initialization logic and conditional routing</name>
  <files>
    src/App.tsx
    src/main.tsx
  </files>
  <action>
    1. Update src/main.tsx:
       - Import createRoot from react-dom/client
       - Import App from ./App.tsx
       - Create root element and render App
       - Standard Vite + React 19 setup
    2. Create src/App.tsx with:
       - Import useEffect, useState from React
       - Import initializeDatabase from src/db/init.ts
       - Import useDisplayName hook from src/hooks/useDisplayName.ts
       - Import WelcomeScreen component (not yet created, but reference it)
       - Import AppShell component (not yet created, but reference it)
       - On mount, call useEffect to run initializeDatabase() once
       - Conditional rendering: if displayName is empty, show WelcomeScreen; else show AppShell
       - Wrap in Suspense boundary (optional for indexeddb loading, but good practice)
       - Include basic error boundary or error logging for database init failures
       - Pass displayName as prop to AppShell and WelcomeScreen as needed
    3. Ensure App.tsx is ~30-40 lines (small, focused on orchestration, not implementation)
  </action>
  <verify>
    1. npm run dev loads without errors
    2. React components can be imported without syntax errors
    3. TypeScript compilation passes (npx tsc --noEmit)
    4. Console logs show database initialization attempt (check browser DevTools console)
  </verify>
  <done>
    App.tsx created with initialization logic. Conditional rendering based on display name (welcome vs. main app). Database initialization triggered on mount. Ready for WelcomeScreen and AppShell implementation.
  </done>
</task>

<task type="auto">
  <name>Create WelcomeScreen and AppShell components with responsive layout</name>
  <files>
    src/components/welcome/WelcomeScreen.tsx
    src/components/layout/AppShell.tsx
    src/hooks/useJournalStorage.ts
  </files>
  <action>
    1. Create src/components/welcome/WelcomeScreen.tsx:
       - Functional component with props { displayName: string; onDisplayNameChange: (name: string) => void }
       - Show warm-toned welcome message (use Tailwind cream/sand/terracotta colors)
       - Input field for display name (text input, required field, min 1 char)
       - Button to confirm (call onDisplayNameChange and trigger update to parent)
       - Styling: center on screen, calm minimal design, no aggressive CTAs
       - Use Tailwind classes: bg-cream, text-charcoal, p-8, md:p-12, rounded-lg
       - Include placeholder text explaining why display name is collected ("For personalization, stored locally on your device")
    2. Create src/components/layout/AppShell.tsx:
       - Functional component with props { displayName: string }
       - Header: show app name and user greeting (e.g., "Welcome back, {displayName}")
       - Content area: placeholder for future features (journaling, deck selection, etc.)
       - Footer: minimal (no aggressive links initially)
       - Responsive layout: full-width on mobile (flex flex-col), 3-col grid on desktop (optional, can be simple stack)
       - Use Tailwind: bg-cream text-charcoal, warm color accents
       - Add border/shadow for cards (border-sage, shadow-sm)
    3. Create src/hooks/useJournalStorage.ts:
       - Import { useLiveQuery } from 'dexie-react-hooks'
       - Import journalDb from src/db/schema.ts
       - Export function useJournalEntries(): JournalEntry[] | undefined
       - Call useLiveQuery(() => journalDb.entries.toArray(), [])
       - Return entries if loaded, undefined if loading (component handles loading state)
       - Include JSDoc explaining that undefined means data is loading
    4. Wire components together:
       - WelcomeScreen calls useDisplayName to get current value
       - AppShell calls useJournalStorage to get entries (placeholder for future display)
       - Both receive displayName prop from App.tsx
  </action>
  <verify>
    1. npm run dev loads the welcome screen on first launch (if no localStorage displayName)
    2. Typing a name and submitting updates App.tsx state and shows AppShell
    3. Refreshing page shows AppShell with saved display name (not welcome again)
    4. npm run build completes without errors
    5. Browser DevTools: check Tailwind classes are applied to elements
    6. Responsive test: shrink browser to mobile width (375px) — layout should reflow without horizontal scroll
  </verify>
  <done>
    WelcomeScreen component created with input for display name. AppShell created with header and content area. useJournalStorage hook created to query IndexedDB entries. All components styled with Tailwind warm palette. Responsive design tested. Ready for checkpoint verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Foundation Phase 1 setup: React + Vite app with Tailwind CSS, IndexedDB persistence via Dexie, localStorage for display name, PWA service worker precaching, welcome screen, and app shell. All three phase requirements (INFRA-02, INFRA-03, UX-05) are implemented with working offline support and responsive layout.
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev (should load on http://localhost:5173 without errors)
    2. First launch verification:
       - Welcome screen appears with input field for display name
       - Type a name (e.g., "Alice") and submit
       - App shell appears with greeting message (e.g., "Welcome back, Alice")
       - No errors in browser console
    3. Persistence verification:
       - Refresh the page (Cmd+R or Ctrl+R)
       - App shell loads immediately without showing welcome screen again
       - Display name greeting persists ("Welcome back, Alice")
       - Check browser DevTools > Application > Local Storage — see app:displayName entry
       - Check browser DevTools > Application > IndexedDB > JournalDB > entries — table exists (may be empty)
    4. Offline verification:
       - Open DevTools > Network tab
       - Switch network to "Offline" mode
       - Refresh page — app should load from service worker cache
       - All CSS and JS should load (no 404 errors)
       - App shell should display (may show cached app shell)
    5. Responsive verification:
       - In DevTools, toggle device toolbar (mobile/tablet view)
       - Set to iPhone 14 (375px width) — layout should reflow without horizontal scroll
       - Set to iPad (768px width) — layout should adjust to tablet spacing
       - Set to desktop (1280px width) — layout should adjust to desktop spacing
    6. TypeScript verification:
       - Run npx tsc --noEmit — should pass with no errors
    7. Production build verification:
       - Run npm run build — should complete without errors, output to dist/
       - Run npm run preview — should serve production build on http://localhost:4173
       - Verify preview loads without errors and welcome/persistence works the same way
  </how-to-verify>
  <resume-signal>
    Type "approved" if all verifications pass, or describe any issues encountered (e.g., "welcome screen doesn't persist displayName", "offline mode shows errors", "layout breaks on mobile").
  </resume-signal>
</task>

<task type="auto">
  <name>Configure PWA manifest and finalize service worker setup</name>
  <files>
    public/manifest.json
    vite.config.ts
  </files>
  <action>
    1. Create public/manifest.json with PWA metadata:
       ```json
       {
         "name": "WNRS Companion",
         "short_name": "WNRS",
         "description": "A local-first journaling app for meaningful guided conversations",
         "start_url": "/",
         "scope": "/",
         "display": "standalone",
         "background_color": "#f5f1e8",
         "theme_color": "#d4a574",
         "icons": [
           {
             "src": "/vite.svg",
             "sizes": "any",
             "type": "image/svg+xml"
           }
         ]
       }
       ```
       - start_url: "/" (app entry point)
       - display: "standalone" (no browser chrome, PWA mode)
       - background_color: cream (#f5f1e8)
       - theme_color: terracotta (#d4a574) — warm color palette
       - icons: placeholder using vite.svg (production can add proper icons later)
    2. Update vite.config.ts PWA plugin config:
       - Ensure manifest reference points to public/manifest.json
       - Set workbox globPatterns to precache all static assets: ['**/*.{js,css,html}']
       - Ensure registerType: 'autoUpdate' (auto-refresh on new version)
       - Add manifest override if needed (can reference from public/ folder)
    3. Verify manifest.json is copied to dist/ during build
  </action>
  <verify>
    1. npm run build completes without errors
    2. dist/manifest.json exists after build
    3. dist/ contains service worker file (generated by vite-plugin-pwa)
    4. npm run preview loads app successfully
    5. Browser DevTools > Application > Manifest tab shows manifest.json loaded with correct metadata
    6. Browser DevTools > Application > Service Workers tab shows service worker registered and active
  </verify>
  <done>
    PWA manifest configured with app metadata, theme colors, and display mode. Service worker precaching setup complete. App is installable via "Add to home screen" prompt and works offline with cached app shell.
  </done>
</task>

</tasks>

<verification>
After all tasks (including checkpoint approval):
1. Full app initialization: npm run dev loads without errors, welcome screen appears on first launch, persists displayName on return visits
2. Database: IndexedDB table exists (JournalDB.entries), localStorage has displayName entry
3. Offline: App loads in offline mode from service worker cache, all assets served from cache
4. Responsive: Layout reflows correctly on mobile (375px), tablet (768px), and desktop (1280px)
5. PWA: Manifest.json correct, service worker active, app installable
6. TypeScript: npx tsc --noEmit passes
7. Production: npm run build && npm run preview works as expected
</verification>

<success_criteria>
- Phase 1 Foundation goals achieved: local-first offline app, IndexedDB persistence, PWA installability
- INFRA-02 met: App functions offline with service worker precaching and sync logic ready for Phase 3
- INFRA-03 met: Journal entries stored in IndexedDB, localStorage for display name
- UX-05 met: App responsive on mobile, tablet, and desktop (web-first, mobile-responsive)
- All three requirements (INFRA-02, INFRA-03, UX-05) have working implementations
- User can see welcome screen, enter display name, and app persists state across restarts
- No TypeScript errors, production build succeeds
- Ready for Phase 2 (Core Solo Experience) which builds on this foundation
</success_criteria>

<output>
After completion and checkpoint approval, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md` documenting:
- Component structure (WelcomeScreen, AppShell, hooks)
- Database initialization flow (when it happens, error handling)
- PWA configuration (manifest, service worker strategy, precache patterns)
- Responsive design approach (Tailwind breakpoints used)
- Display name persistence strategy (localStorage)
- Any deviations from research and rationale
- What Phase 2 should expect to build on (working app shell, database ready, hooks for data access)
</output>
