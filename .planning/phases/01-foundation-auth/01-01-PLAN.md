---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - tailwind.config.ts
  - src/main.tsx
  - src/styles/globals.css
  - .gitignore
autonomous: true
requirements:
  - UX-05

must_haves:
  truths:
    - "App initializes without errors in development mode"
    - "Tailwind CSS classes resolve in components"
    - "TypeScript types are checked without strict mode blocking normal code"
  artifacts:
    - path: "package.json"
      provides: "React 19, Vite 7, Tailwind CSS 4, TypeScript 5 dependencies"
    - path: "tsconfig.json"
      provides: "TypeScript configuration with normal mode (noImplicitAny only)"
    - path: "vite.config.ts"
      provides: "Vite development and build configuration with React plugin"
    - path: "tailwind.config.ts"
      provides: "Tailwind theme with warm/soft color palette (cream, sand, terracotta, sage)"
    - path: "src/styles/globals.css"
      provides: "Tailwind directives and base styles"
  key_links:
    - from: "package.json"
      to: "vite.config.ts"
      via: "dependency resolution"
      pattern: "react.*19.*vite.*7"
    - from: "vite.config.ts"
      to: "tsconfig.json"
      via: "TypeScript configuration"
      pattern: "tsconfig"
    - from: "tailwind.config.ts"
      to: "src/styles/globals.css"
      via: "Tailwind directives"
      pattern: "@tailwind"
---

<objective>
Initialize React + Vite project with Tailwind CSS, TypeScript, and development tooling configured. This establishes the foundation for all subsequent development.

Purpose: Create a working development environment that supports cross-platform (web + mobile-responsive) development with type safety and fast hot module reloading (HMR).

Output: A runnable Vite SPA with Tailwind CSS configured, TypeScript in normal mode, and a clean project structure ready for database and PWA setup.
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Vite React project with dependencies</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.node.json
    index.html
    src/main.tsx
    src/App.tsx
    .gitignore
  </files>
  <action>
    1. Delete existing codebase if present (user decision: "can be deleted/replaced — start fresh if needed")
    2. Create a new Vite React project using: npm create vite@latest . -- --template react
    3. Install dependencies:
       - npm install react@19 react-dom@19
       - npm install -D @vitejs/plugin-react
       - npm install -D typescript @types/react @types/react-dom @types/node
       - npm install -D tailwindcss @tailwindcss/vite
    4. Create vite.config.ts with React and Tailwind plugins:
       - Use defineConfig from 'vite'
       - Include @vitejs/plugin-react() plugin
       - Include @tailwindcss/vite plugin (latest Tailwind CSS 4 syntax)
       - Set build output to 'dist'
    5. Update tsconfig.json:
       - Set strict: false (normal mode per user decision)
       - Set noImplicitAny: true (catch untyped functions even in normal mode)
       - Set noImplicitThis: true
       - Set target: ES2020, module: ESNext
       - Set jsx: react-jsx (modern React 19 transform)
       - Include vite/client types
    6. Create src/App.tsx with a simple placeholder component (will be replaced by core UI plan)
    7. Update .gitignore to exclude node_modules/, dist/, .env, .env.local
  </action>
  <verify>
    1. npm install completes without errors
    2. npm run dev starts the dev server on http://localhost:5173
    3. Browser loads the Vite React app without TypeScript errors
    4. npm run build completes successfully, outputs to dist/
    5. TypeScript compiler shows no errors (npx tsc --noEmit)
  </verify>
  <done>
    Vite + React 19 + TypeScript (normal mode) initialized with all dependencies installed. App is runnable in development (npm run dev) and production builds (npm run build). No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Configure Tailwind CSS with warm color palette and responsive design</name>
  <files>
    tailwind.config.ts
    src/styles/globals.css
    postcss.config.js
  </files>
  <action>
    1. Create tailwind.config.ts (TypeScript syntax per user decision):
       - Extend theme colors with warm/soft palette: cream (#f5f1e8), sand (#e8dcc8), terracotta (#d4a574), sage (#a8b5a0), slate (#6b7280), charcoal (#2d3436)
       - Extend fontFamily with serif (Crimson Text, Georgia) and sans (Inter, system-ui)
       - Include content paths: ['./index.html', './src/**/*.{js,ts,jsx,tsx}']
       - Set spacing extension: 128 → 32rem (for larger layouts)
       - Ensure Tailwind CSS 4 compatibility (no legacy format)
    2. Create src/styles/globals.css with Tailwind directives:
       - @tailwind base; @tailwind components; @tailwind utilities;
       - Add optional base styles for body, html (e.g., font-sans, bg-cream)
    3. Create postcss.config.js (Tailwind 4 uses @tailwindcss/vite plugin, but postcss.config.js can be minimal or omitted per Tailwind docs; include if build requires it)
    4. Update src/main.tsx to import src/styles/globals.css
    5. Verify Tailwind classes work in App.tsx by adding a simple element with className="bg-cream text-charcoal" or similar
  </action>
  <verify>
    1. tailwind.config.ts is syntactically valid (npx tsc --noEmit on config file)
    2. npm run dev loads the app and Tailwind classes apply (inspect element to see applied styles)
    3. Warm color classes (cream, sand, terracotta, sage) render correctly in browser DevTools
    4. Responsive breakpoints (sm, md, lg) work when browser is resized (check Tailwind mobile-first design)
    5. npm run build includes Tailwind CSS in the final dist/ bundle
  </verify>
  <done>
    Tailwind CSS 4 configured with custom warm/soft color palette and typography. Global styles imported and applied. Responsive design system ready (mobile-first breakpoints). Custom colors available for all subsequent components.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `npm run dev` — app loads on http://localhost:5173 without errors
2. Run `npx tsc --noEmit` — TypeScript compilation succeeds (no errors, normal mode allows flexibility)
3. Run `npm run build` — production build succeeds, outputs to dist/
4. Verify project structure matches research recommendations (src/, vite.config.ts, tsconfig.json in place)
5. Confirm Tailwind CSS classes are available in DevTools (inspect element shows Tailwind classes applied)
</verification>

<success_criteria>
- Vite dev server runs successfully with React 19 and Tailwind CSS working
- TypeScript in normal mode (strict: false) with noImplicitAny enabled
- Custom warm/soft color palette available in Tailwind classes
- Production build completes without errors
- Project structure ready for database schema and PWA setup in next plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md` documenting:
- Vite config details (plugins, build output)
- Tailwind theme colors used
- TypeScript mode and key settings
- Import paths and file structure established
- Any deviations from research recommendations and why
</output>
