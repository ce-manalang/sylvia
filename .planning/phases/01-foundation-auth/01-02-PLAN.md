---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/db/schema.ts
  - src/db/init.ts
  - src/types/journal.ts
  - tsconfig.json
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Journal entries can be saved to IndexedDB and persisted across page refresh"
    - "Database schema includes question, answer text, emotion tag, and timestamp fields"
    - "Local display name persists in localStorage for personalization"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Dexie database class with JournalEntry table and indexes"
      min_lines: 25
    - path: "src/db/init.ts"
      provides: "Database initialization function with error handling"
      min_lines: 15
    - path: "src/types/journal.ts"
      provides: "TypeScript interface for JournalEntry with required fields"
      contains: "interface JournalEntry"
    - path: "package.json"
      provides: "Dexie 4.3.0 and dexie-react-hooks dependencies"
      contains: "dexie"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/db/init.ts"
      via: "journalDb instance exported and initialized"
      pattern: "journalDb.open\\(\\)"
    - from: "src/types/journal.ts"
      to: "src/db/schema.ts"
      via: "JournalEntry interface used in Dexie table definition"
      pattern: "Table<JournalEntry>"
    - from: "src/db/init.ts"
      to: "src/App.tsx"
      via: "initialization on app startup (will be wired in next plan)"
      pattern: "initializeDatabase\\(\\)"
---

<objective>
Set up IndexedDB schema using Dexie for persistent journal storage and localStorage for user display name. This provides the data persistence foundation for offline-first functionality.

Purpose: Enable local data storage without a backend, allowing users to write and retrieve journal entries that survive app restarts and network disconnection. Storage strategy meets INFRA-02 (offline function) and INFRA-03 (local journal storage).

Output: Working IndexedDB schema with indexes for querying, initialization function, and TypeScript types for journal data. localStorage hook for display name persistence.
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Set up Dexie IndexedDB schema for journal entries</name>
  <files>
    package.json
    src/db/schema.ts
    src/db/init.ts
    src/types/journal.ts
  </files>
  <action>
    1. Install dependencies:
       - npm install dexie
       - npm install dexie-react-hooks
    2. Create src/types/journal.ts with TypeScript interface:
       ```typescript
       export interface JournalEntry {
         id?: number;
         questionId: string;
         answer: string;
         emotionTag: string; // e.g., 'hopeful', 'anxious', 'neutral', 'thoughtful', 'connected'
         timestamp: number;
         updatedAt?: number;
         // Forward compatibility for Phase 3:
         cloudId?: string;
         syncedAt?: number;
       }
       ```
    3. Create src/db/schema.ts with Dexie database class:
       - Import Dexie and JournalEntry type
       - Define JournalDatabase class extending Dexie
       - Add entries table with indexes: primary key (++id), timestamp (for sorting), questionId (for queries)
       - Version: 1
       - Export singleton instance: journalDb
       - Example: `this.version(1).stores({ entries: '++id, timestamp, questionId' });`
    4. Create src/db/init.ts with initialization function:
       - Export async function initializeDatabase()
       - Call journalDb.open()
       - Wrap in try/catch, log success/error to console
       - Return void (called during app startup)
    5. Per research: Reserve schema space for forward compatibility (cloudId, syncedAt fields for Phase 3 sync) but don't implement sync logic yet
  </action>
  <verify>
    1. npm install completes without errors
    2. TypeScript compilation succeeds (npx tsc --noEmit)
    3. Dexie database class loads without errors by importing in a test file
    4. IndexedDB quota checks pass (no immediate errors on db access)
  </verify>
  <done>
    Dexie IndexedDB schema created with JournalEntry table indexed by id, timestamp, and questionId. Initialization function ready to call on app startup. TypeScript types defined with forward-compatible fields (cloudId, syncedAt reserved for Phase 3).
  </done>
</task>

<task type="auto">
  <name>Create localStorage hook for display name persistence and PWA plugin setup</name>
  <files>
    src/hooks/useDisplayName.ts
    package.json
    vite.config.ts
  </files>
  <action>
    1. Install PWA dependencies:
       - npm install -D vite-plugin-pwa
    2. Create src/hooks/useDisplayName.ts with React hook:
       - Use useState with localStorage initializer callback (synchronous read on mount)
       - Store key: 'app:displayName'
       - Export function useDisplayName(): { displayName: string; updateDisplayName: (name: string) => void }
       - updateDisplayName saves to localStorage and updates state
       - No network calls, fully client-side
       - Include JSDoc comment explaining usage
    3. Update vite.config.ts to add VitePWA plugin:
       - Import { VitePWA } from 'vite-plugin-pwa'
       - Add VitePWA plugin to plugins array with:
         - strategies: 'generateSW' (per research recommendation)
         - registerType: 'autoUpdate' (auto-refresh when new version deployed)
         - workbox.globPatterns: ['**/*.{js,css,html}'] (precache app shell)
         - manifest (basic PWA metadata — will be expanded in next plan)
    4. Verify React hook imports correctly and doesn't reference non-existent modules
  </action>
  <verify>
    1. useDisplayName hook is importable without errors
    2. localStorage persists data across browser refresh (manual test in browser DevTools > Application > Local Storage)
    3. npm run build includes vite-plugin-pwa without errors
    4. vite.config.ts syntax is valid (npx tsc --noEmit on config)
  </verify>
  <done>
    useDisplayName React hook created for localStorage-backed user name persistence. vite-plugin-pwa installed and configured with generateSW strategy for app shell precaching. Hook and PWA config ready for integration in core UI components.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `npx tsc --noEmit` — no TypeScript errors
2. Import both schema.ts and useDisplayName.ts in a test file — verify they load without errors
3. Verify Dexie is available in package.json (npm list dexie)
4. Verify vite-plugin-pwa is configured in vite.config.ts (check VitePWA plugin syntax)
5. Confirm schema includes forward-compatible fields (cloudId, syncedAt) for Phase 3 migration
</verification>

<success_criteria>
- Dexie IndexedDB schema with JournalEntry table ready (id, timestamp, questionId indexes)
- useDisplayName hook persists display name to localStorage
- TypeScript types defined and exported correctly
- vite-plugin-pwa integrated into Vite config with generateSW strategy
- All code passes TypeScript compilation (normal mode)
- Database and hooks ready to wire into React components in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md` documenting:
- Dexie schema design (table name, indexes, field definitions)
- IndexedDB initialization strategy
- localStorage hook implementation details
- PWA plugin configuration (strategies, precache patterns)
- Forward-compatible schema fields reserved for Phase 3
- Any deviations from research and rationale
</output>
